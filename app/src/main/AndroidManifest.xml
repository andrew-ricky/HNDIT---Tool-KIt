<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.HNDITAcademicToolkit">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".GpaActivity"
            android:exported="false"
            android:label="@string/title_gpa" />

        <activity
            android:name=".TargetGpaActivity"
            android:exported="false"
            android:label="@string/title_target_gpa" />

        <activity
            android:name=".AttendanceActivity"
            android:exported="false"
            android:label="@string/title_attendance" />

        <activity
            android:name=".CaPredictorActivity"
            android:exported="false"
            android:label="@string/title_ca_predictor" />

    </application>

</manifest>
